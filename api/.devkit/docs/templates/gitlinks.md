# gitlinks

The `gitlinks` tool is used to manage and validate git submodules and their
corresponding gitlinks within a project.

It ensures that submodules are in a consistent state and that the parent
repository's gitlinks point to the correct remote branches as defined in
`.gitmodules`.

## Usage

```
{% include 'help/gitlinks.txt' %}
```
